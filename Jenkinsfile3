pipeline {
    agent any
    
    stages {
        stage('Build and run Nginx container') {
            steps {
                script {
                    // Запуск контейнера Nginx
                    docker build .
                    docker run -d -p 8181:80 
                    )
                }
            }
        }
    }
}
